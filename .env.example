# Example environment variables (DO NOT commit real secrets)
# Copy this file to `.env` and fill values locally or in your hosting provider.

# Stripe (live keys for production, test keys for development)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Email provider (Resend) API key
RESEND_API_KEY=

# Sentry (monitoring) DSN - optional
SENTRY_DSN=

# Site domain
DOMAIN=http://localhost:3000
